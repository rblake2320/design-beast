# beast library (media organizer) lane — on top of requirements.txt.
# torch must be a cu128 build on Blackwell (RTX 5090): pip install torch --index-url https://download.pytorch.org/whl/cu128
imagehash==4.3.2
pillow-heif
insightface>=0.7
onnxruntime-gpu>=1.20
easyocr==1.7.2
transformers>=4.51
psycopg[binary]>=3.2
pgvector>=0.3
