"""Beast Library — organize, deduplicate, index, review and search a media collection.

Deterministic tools do inventory, hashing, and file operations; SigLIP2 does the
high-volume indexing; a local VLM (Ollama) is the semantic judge for cluster
representatives only. Originals are never deleted or overwritten.
"""
