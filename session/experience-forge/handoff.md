# Handoff

Resume with `python scripts/beast_core.py recover session/experience-forge/latest.json --allow-head-drift`. Verify `proofs/experience-forge/signed-evidence.jsonl` with `scripts/verify_signed_evidence.py --evidence-root .`, rerun the full suite, and review the draft PR against the proof boundaries. Do not merge the builder's own PR; no production signing key, scheduler, GPU model, or third-party design skill was activated.
