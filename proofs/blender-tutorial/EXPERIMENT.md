# Fresh Blender tutorial experiment

- Frozen runner: `aac9d29` (PR #32; both GitHub checks passed).
- Source selected before pixel inspection: https://www.youtube.com/watch?v=m9c80LWLa-8
- Metadata: blenderian, "Blender how to bend a cube", 135 seconds.
- Selection rationale: small public geometry procedure close to the bounded
  runner's documented primitive/transform/bend operations. No selection based on
  a passing outcome. This is an adjacent-task experiment, not randomized breadth.
- Rights: no redistribution license returned by metadata. Full video and frames
  stay local under gitignored `watched/`; retain source URL, timestamps and hashes.
- Fixed acceptance: all essential demonstrated operations represented, no guessed
  numeric values, exact source-derived typed values, nonempty saved and reopened
  scene, native geometry/transform/modifier checks, retained diagnostic views,
  independent source-to-result inspection. Report source-version drift explicitly.
- Unknowns: video pixels not yet inspected; subdivision/apply-transform/other
  actions may exceed v1. Missing support must fail before a pass is claimed.
- Repair rule: preserve frozen-run failure and original input before changing
  implementation. A repaired same-tutorial run is not held-out validation.
- No paid generations, editor-project mutations, or new GPU workloads.
