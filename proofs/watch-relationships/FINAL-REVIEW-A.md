# Final independent artifact review A

Reviewed commit e2fa3571f891ca212b0859124a0c67dc1a5e98c1 for draft PR43 in D:/content/design-beast-relationships. Initial worktree status was clean and HEAD matched that commit. This appended review is intentionally outside the earlier inventory. Original reviews and frozen labels were not altered.

## Bounded decision

ACCEPT the retained artifact package as evidence of the reported known-case tie and graduation FAIL. No blocking discrepancy was found in the reviewed committed evidence. This is not merge approval, fresh blind superiority validation, causal recovery, or production readiness. The single-native-frame miss remains a measured failure.

## Custody and execution lineage

`python scripts/verify_watch_relationships.py` returned verified: 4,134 files, 72,700,449 bytes. I additionally compared all 4,134 manifest Git blob identifiers against `git ls-tree -r e2fa3571f891ca212b0859124a0c67dc1a5e98c1`; every identifier matched. Thus this review checked actual committed tree membership in addition to local manifest consistency.

All 20 final SCORE report SHA-256 values matched retained reports. The scorer SHA-256 matched both current working bytes and the exact HEAD Git blob. All ten baseline reuse receipts matched both the reused report and the original ocr-repair baseline report. Final conditional execution is attributed to 1248ec5, while baseline remains the retained 0f11140 execution; these are not paired timing samples.

Exact source-byte distinction: the evaluator and relationships receipt hashes match raw Git blobs at their respective execution commits. The six imported dependencies (observe_watch_input_overlays.py, rewind.py, inspection.py, inspection_runtime.py, seek.py, core.py) have receipt hashes matching CRLF checkout bytes and current working files. Their raw committed blobs use LF; applying LF-to-CRLF checkout normalization reproduces the receipt hashes. This is an explained newline representation difference, not a demonstrated logic difference; do not describe all eight receipt hashes as raw committed blob SHA-256 values.

## Guard and budget checks

The final prior_control implementation requires stamp < event timestamp and chooses a unique latest matching prior label. Every final conditional control reference checked obeyed that strict ordering. Pointer-containing panels cannot populate the known-controls cache; a pointer-covered pressed control lacking prior support is cleared to unknown. Spatial grouping preserves conflicts instead of turning differing OCR strings into extra held-input events.

All ten final conditional inspection receipts charged exactly 5 initial + 2 control-context + 9 rewind requests, totaling 16. OCR remained at or below 16 and wall time below 120 seconds per arm. Shared source hashes, snapshot-bound pixel routing, and source-derived mosaic OCR remain in place. All inspected summaries preserve causal sufficiency unknown, no accepted causal chains, and no publication authority. Temporal completeness is the only early-stop claim.

Focused test command: `python -m pytest tests/test_watch_relationships.py -q -p no:cacheprovider`, with PYTHONDONTWRITEBYTECODE=1, returned 25 passed in 0.40 seconds. No media extraction, OCR invocation, or observer rerun was performed by this reviewer.

## Direct visual checks

- Case05: final conditional frame at requested 2160 ms visibly shows Save down while Saved is already present. The report records Saved from 810 ms and no temporal candidate. This agrees with independently labeled result-before-input evidence.
- Case06: final conditional frames at requested 1090 and 2180 ms visibly show Inspect down followed by Save down, with Idle at both inspected instants. The report preserves two inputs and two order-compatible candidates for later Saved. It does not claim which input caused Saved.
- Case09: final conditional frames at requested 1100 and 2200 ms visibly show Save down then Inspect down. The earlier OCR-driven extra fragment is absent from this final report. Both candidates remain causal-unknown alternatives.
- Case08: native capture frames f-0034.jpg, f-0035.jpg and f-0036.jpg show unpressed, Save down, unpressed at native PTS 1320, 1360 and 1400 ms. Both final reports have no input event, so the single-native-frame miss is real and remains counted. Requested extraction times are distinct from native PTS and retain the declared 40 ms scoring tolerance.

These inspected frames are all covered by the verified committed inventory. Existing blind-review frame hashes and native PTS remain the reference labels.

## Claim boundary

The retained score reports each arm at 10/11 recovered events, 10 correct control identities, 10/10 result identities, and 9/10 temporal pairs, with no false event extras or false temporal pairs. Conditional adds zero recovered events over baseline, so the frozen superiority requirement fails. Unknown labels and unconditional abstention do not test positive causal discrimination. The documented series of OCR, routing and context repairs used known cases; further superiority claims require a new frozen evaluation set. No additional same-set repair is requested by this review.
